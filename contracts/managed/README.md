# Generated Compact artifacts

This directory is intended to contain the generated Compact outputs after running:

```bash
compact compile contracts/skillgate.compact contracts/managed/skillgate
```

The compile step will generate circuit files, keys, and related artifacts here.
